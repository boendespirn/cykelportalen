# issues.md — fejl- og opgavelog

Den eneste kilde til sandhed for problemer og opgaver på tværs af kørsler (jf. `PROTOKOL.md`). Direktøren er eneste skriver. Hver kørsel læses denne fil først.

**Statusser:** `NY → TILDELT → ESKALERET → AFVENTER_EJER → LØST` (samt `HENLAGT`).
**Prioritet:** HØJ / MIDDEL / LAV.

Direktøren udvider hvert issue med dato, forsøg og løsning, efterhånden som der arbejdes.

| ID | Prioritet | Status | Ansvarlig | Beskrivelse |
|----|-----------|--------|-----------|-------------|
| SIK-001 | HØJ | LØST | ejer | Hardkodet CF-login flyttet til `os.getenv(CF_EMAIL/CF_PASSWORD)` i koden; ejer har skiftet password og sat env vars i Railway. Løst 2026-06-28. |
| SIK-002 | HØJ | LØST | ejer | Alle eksponerede nøgler/tokens roteret og opdateret i Railway. Løst 2026-06-28. |
| SIK-003 | MIDDEL | LØST | kontrolagent | Audit af `agents/` (inkl. alle `- Kopi.py`-dubletter) for hardkodede nøgler/tokens gennemført 2026-07-04 — grep for kendte token-formater + literal password/api_key/token/secret-mønstre. Intet fundet ud over ét issue, udskilt som SIK-004. |
| SIK-004 | HØJ | LØST | direktør | **SIK-001 blev kun rettet ét sted:** `agents/profile_reader_agent.py:37-38` havde stadig hardkodet ClimbFinder-login i klartekst (`CF_EMAIL`/`CF_PASSWORD`), git-committet (senest rørt i `8dc3f1f` uden at disse linjer blev ændret) — mens `climbfinder_agent.py` korrekt bruger `os.getenv`. Fundet af kontrolagenten 2026-07-04 under SIK-003-audit. Rettet samme dag: ændret til `os.getenv("CF_EMAIL", "")` / `os.getenv("CF_PASSWORD", "")`, matcher nu `climbfinder_agent.py`s mønster. **Kræver stadig:** ejeren bør rotere ClimbFinder-passwordet igen, da strengen har ligget i git-historikken siden før SIK-001 blev lukket. |
| STG-001 | HØJ | LØST | stigningsagent + direktør | Tre rod-bugs fundet og rettet 2026-07-03 for tour-de-france-2026: (1) `stage_pcs_agent.py` valgte blindt første `/profiles/`-billede på PCS-siden — rettet med filnavns-filter. (2) `climbfinder_agent.py` ryddede aldrig gamle forkerte billede-matches efter `SEARCH_OVERRIDES[navn]=None` — 10 stigninger viste billeder fra helt andre bjerge (fx Côte de Larringes viste et billede fra Pau). Tilføjet `clear_stale_override_images()`. (3) **Rettelse til tidligere status:** stigningsagentens første rapport konkluderede fejlagtigt "20/21 etaper korrekte" — ejeren opdagede efterfølgende, at etape 5 og 9 stadig viste rutekort. Rodårsagen var en TREDJE bug: `elevation_image_agent.py` kører uafhængigt af `stage_pcs_agent.py` (scraper PCS's `/info/profiles`-underside separat) og overskriver derfor altid det, `stage_pcs_agent.py` fandt — dens eget filnavns-filter var lige så upålideligt. Rettet 2026-07-03 ved at vælge mindste kandidat (byte-størrelse) blandt filnavns-matchende billeder (ægte profiler: 60-120 KB, rutekort: 300 KB+, verificeret empirisk). Alle 21 etaper nu **visuelt verificeret én for én** (ikke kun stikprøve) — 20/21 korrekte, etape 3 har ægte PCS-datamangel (se STG-002). |
| STG-002 | MIDDEL | LØST | stigningsagent | Etape 3 (tour-de-france-2026, Granollers–Les Angles) manglede hel-etape-højdeprofil (ægte PCS-datamangel). Løst 2026-07-04: nyt script `agents/stage_profile_generator.py` genbruger GPX-kilde og render-logik fra `climb_profile_generator.py`, men for hele etapens GPX-spor, med kendte stigninger overlejret som markerede bånd. Håndterer ~4.5% GPX-vs-officiel distance-afvigelse via samme skaleringsfaktor som climb-generatoren. Visuelt verificeret (alle 4 stigninger rammer korrekt), skrevet til produktion kun for etape 3 (ingen andre TdF/giro/dauphiné/tour de suisse-etaper manglede et hel-etape-billede). Fandt og rettede desuden en latent STG-007-lignende regressionsrisiko: `stage_pcs_agent.py`s upsert inkluderede altid `elevation_image_url`, så en fremtidig PCS-rescrape ville have nulstillet det nye billede — rettet til at udelade feltet når PCS ikke selv finder noget. Committet `74bb193`, pushet. |
| STG-003 | MIDDEL | LØST | stigningsagent | Rodårsag bekræftet: `profile_reader_agent.py` gensætter aldrig `gradient_sections` når den erstatter data med Claude-vision — **149/149 rækker i hele DB var NULL**, hvilket gjorde 34 stigninger (3 i TdF) helt usynlige i frontend (`hasVisualProfile()` kræver enten billede ELLER gradient_sections). Rettet 2026-07-04: ny `generate_gradient_sections()`-funktion tilføjet, samt backfill-script `agents/fix_gradient_sections_backfill.py` (dry-run default) kørt mod hele DB — 129/149 skrevet (0 fejlede), 20 rækker manglede reelt `length_km`/`avg_gradient` og korrekt sprunget over frem for gættet. Committet `7e0d6a6`, pushet. |
| STG-014 | HØJ | LØST | stigningsagent | **Ny regression fundet under STG-009-arbejde:** `climbfinder_agent.py`s `clear_stale_override_images()` og hovedløkkens "ryd forkastet match"-logik ryddede ubetinget ETHVERT eksisterende billede, når intet nyt CF-match kunne verificeres — også GPX-genererede fallback-billeder (`source="generated"`) fra `climb_profile_generator.py`. Havde gentagne gange slettet fallback-billeder for Côte de Loucroup, Gavarnie-Gèdre, Les Angles og Côte de l'Estadi Olímpic, uafhængigt af `--overwrite`. Rettet 2026-07-04: begge steder guardes nu mod `source="generated"`, og `update_climb_profile()` sætter altid `source="vision"` når den selv skriver et match. Alle 4 ramte billeder genskabt. Committet `4e50ff9` (samme commit som STG-009), pushet. |
| STG-009 | HØJ | LØST | stigningsagent | **STG-004-fejlklassen ("rigtige tal, forkert bjerg") regredierede — nu rettet med hård geografisk cross-check.** Ny `geo_ok()` i `climbfinder_agent.py` sammenligner CF-kandidatens egne lat/lng mod det GPX-udledte summit og afviser hårdt ved >50 km afstand (ægte match lå 2-20 km væk, forkerte 190-9000 km). Kandidatpuljen udvidet til top-5 (kun når en GPX-summit findes at hård-verificere imod), bevarer CF's egen relevans-rækkefølge for ikke at vælge en nærmere-men-forkert variant af allerede korrekte match (Col du Tourmalet, Col d'Aspin — verificeret upåvirkede). Ny `SEARCH_OVERRIDES["Col du Calvaire"]` tilføjet, da CF's rækkefølge for det navn ikke var stabil på tværs af kald. **Fuld re-verifikation af hele tour-de-france-2026 (--overwrite, ikke stikprøve):** 39 opdateret, 25 korrekt afvist/sendt til GPX-fallback, 0 fejl. Rettet: Côte de Begues → Puig Bernat from Olesa de Bonesvalls, Col de Toses → La Pia from Alp, Col du Calvaire → Col du Calvaire de Font Romeu. Committet `4e50ff9`, pushet. |
| STG-010 | MIDDEL | LØST | stigningsagent | Bekræftet via 4 uafhængige ClimbFinder-ruter til samme pas (alle rapporterer `finishElevation=423` med vidt forskellig reel gain 417/311/366/327m) — 423 er topkote, ikke gain. Direkte DB-rettelse (verificeret af direktøren direkte mod Supabase): `elevation_m` 423→352 (=længde×hældning×10, samme metode som STG-007). Côte d'Engins (STG-013) er bevidst IKKE rørt — afventer stadig ejerens godkendelse. Ét nyt grænsetilfælde fundet (Col de Villerouge, etape 4, ratio 1.57) men bevidst ikke rettet — ingen uafhængig evidens for at det er forkert, i modsætning til denne og STG-013. |
| STG-011 | HØJ | LØST | stigningsagent | Rodårsag: `gpx_climb_agent.py` og `profile_reader_agent.py` sprang begge helt uden om etaper med `stage_type="flat"` — men PCS klassificerer ofte en sprinteretape som "flat" på etape-niveau, selvom den har en kort kategoriseret klatring nær mål (Côte de Baleix, cat. 3, etape 5). Rettet: kun `tt`/`itt` springes nu over (Claude vision returnerer selv tom liste for reelt flade etaper). Kørt for alle 6 flate-flaggede etaper (5,7,8,11,12,21) + etape 17 (aldrig behandlet): **16 tidligere usynlige stigninger fundet og indsat**, alle verificeret/genereret. **TdF står nu på 87/88 stigninger med verificeret billede** — kun Côte d'Engins (STG-013) mangler, afventer stadig ejeren. **Sidefund:** `profile_reader_agent.py`s egen ClimbFinder-søgning havde intet metrics-/geo-tjek og satte forkerte billeder (bekræftet etape 17) — den skriver ikke længere `profile_image_url` selv; kun `climbfinder_agent.py` (nu med geo-tjek) gør det. Committet `1a63a15`, pushet. |
| STG-012 | MIDDEL | LØST | stigningsagent | Rodårsag: `stage_info_agent.py`s prompt sender allerede de præcise tal til Claude, men intet forhindrede parafrasering/afrunding i selve teksten. Rettet: prompten kræver nu præcis brug af tallene, hellere undlade et konkret tal end gætte forkert. Regenereret alle 21 tour-de-france-2026-etaper; stikprøve (etape 1, 2, 10, 21) bekræfter tekst og stat-bokse nu stemmer overens. Committet `02b0403`, pushet. |
| STG-004 | HØJ | LØST | direktør | **Ejeren opdagede manuelt, at ingen af ClimbFinder-billederne for etape 2 matchede de rigtige stigninger** — krydstjekket i `metrics_ok()` fangede det ikke. Rodårsag: tolerancerne var absurd løse (længde 0.5-2.0×, højde ±300-450m, hældning ±4%) — "Côte de Begues" (DB: 6.3 km) blev godkendt mod ClimbFinders "Côte de Benagues" (3.2 km, en helt anden bakke i Frankrig) fordi ratio 0.51 lige akkurat lå inden for båndet. Strammet 2026-07-03 til 0.75-1.33× / ±100m / ±1.5%. Fandt OGSÅ en ny variant af den samme fejlklasse: når intet nyt match kunne verificeres, blev et tidligere (nu forkastet) billede aldrig ryddet — rettet, så et manglende match nu altid nulstiller et eksisterende billede. Genkørt for hele TdF: 31 stigninger fik et stramt verificeret match, 41 blev korrekt afvist/nulstillet. Côte de Begues manuelt ryddet i DB som en direkte konsekvens. **Kræver:** en fuld ny gennemkørsel af hele TdF (og siden hele databasen) for at fange evt. andre stadig-forkerte matches, som denne kørsel ikke nåede at reprocessere — afventer næste session pga. tid/omkostning i denne. |
| OPS-005 | LAV | LØST | direktør | `elevation_image_agent.py` fandtes ikke i git-historikken — kun uncommitted i ejerens arbejdsmappe, med reel risiko for at gå tabt. Committet 2026-07-03 (fundet og reddet af stigningsagenten under STG-001-arbejde). Bemærk: `agents/`-mappen indeholder stadig en rod af dobbelt-scripts (rod + `agents/*.py` + `agents/* - Kopi.py`); bør ryddes op på et tidspunkt, men er bevidst ikke rørt for at undgå at introducere fejl uden for scope. |
| SEO-001 | HØJ | TILDELT | seo-agent | Google indekserer ikke siden. **Kritisk fund 2026-07-04:** frisk GSC-data viste at `https://klassementet.dk/tour-de-france-løb` — en URL der matcher intet rigtigt løb — stod for 190 af ~350 impressions (28 dage), heriblandt vores north-star-søgeord, alle 0% CTR, fordi siden 404'er (jf. SEO-09). URL Inspection bekræftede at `/tour-de-france-2026` og `/tour-de-france-2026/stage/1` var **"unknown to Google" — aldrig crawlet**. **Rettet:** permanent redirect af det døde alias (`58d0855`). Ejeren anmodede manuelt om indeksering i Search Console → URL Inspection for `/tour-de-france-2026`, `/tour-de-france-2026/stage/1`, `/races`, `/nyheder` (2026-07-04). **Frisk opfølgningskontrol 2026-07-06 (seo-agent, via `agents/gsc_agent.py`):** af 26 kontrollerede URL'er (forside + 4 kernesider + etape 1-21) er kun **3 reelt indekseret** — forsiden, `/tour-de-france-2026`, og uventet `stage/16` (indekseret siden 3. juni, før dette SEO-arbejde overhovedet startede). `stage/1` er rykket fra "unknown" til **"Discovered - currently not indexed"** (reel fremgang, men stadig ikke crawlet). `/races` og `/nyheder` står **uændret** som "unknown to Google" to dage efter anmodningen. Af etape 2-21: 9 er "Discovered - currently not indexed" (2,3,4,5,6,7,9,10,21), 10 er stadig "unknown to Google" (8,11-15,17-20). Sitemap er fejlfri (0 fejl/advarsler). **Vurdering:** mønsteret peger på et autoritets-/intern linking-problem, ikke et teknisk crawl-problem — at `stage/16` blev indekseret uden særlig indsats understøtter dette (jf. `STRATEGI.md`s diagnose af "den eksistentielle flaskehals"). **Status:** forbliver TILDELT. Seo-agenten har foreslået en kalenderdrevet intern linkplan der prioriterer de "Discovered - not indexed"-etapesider — afventer direktørens/ejerens gennemgang som separat leverance. **Opfølgning 2026-07-07:** frisk GSC-kontrol viser fortsat kun 3/26 reelt indekseret (uændret), men churn i "Discovered"-bagsen (8 i dag mod 10 i går, ikke samme URL'er) — bekræfter fortsat autoritets-/linking-diagnosen, ingen teknisk regression. Se SEO-013 og SEO-014 for to konkrete delfund fra samme kontrol. |
| SEO-010 | LAV | LØST | seo-agent | IndexNow kaldte tidligere kun ved artikel-godkendelse. Løst 2026-07-04: ny `notify_indexnow()`-funktion tilføjet til både `race_prep_pipeline.py` (kaldes efter alle 7 pipeline-trin, slår løbs-id op og sender løbs-/alle etape-URL'er) og `daily_update.py` (kaldes kun når startliste/etapedata reelt blev opdateret for det løb, via nyt `race_changed`-flag, så det ikke bliver spammy). Genbruger `submit_indexnow()` fra `api.py` — ingen duplikeret logik. Testet isoleret mod tour-de-france-2026 (22 URL'er: løbsside + 21 etapesider), rent additivt diff (82 linjer, 0 fjernet). **Sidefund (ikke rettet, nyt separat issue):** se OPS-007. |
| OPS-008 | HØJ | LØST | direktør | **Ufuldstændig `agents/`-migrering udgjorde en tikkende regressionsrisiko for `race_prep_pipeline.py`.** Fundet af stigningsagenten 2026-07-04 under STG-011-arbejde. **Rettet 2026-07-05:** ved sessionsstart var roden i en langt værre tilstand end da issuet blev logget — en ekstern (ikke-Claude) bulk-kopiering havde efterladt ~50 `" - Kopi"`-dubletter i `agents/` og fysisk slettet 16 rod-originaler uden `git mv`, hvilket bekræftede det eksakte scenarie issuet advarede om: `agents/climbfinder_agent.py` og `agents/pcs_profile_image_agent.py` fandtes slet ikke under deres rigtige navn (kun forældede `- Kopi`-udgaver), så `race_prep_pipeline.py`s trin 3 og 6 ville være fejlet håndgribeligt ved næste løbsforberedelse (fx Vuelta). Løsning: for hver dublet blev rod- og `agents/`-udgaven diffet mod hinanden og mod `git show HEAD:<fil>` for at afgøre hvilken side var ægte/nyeste (roden var i alle tilfælde den korrekte, ubrudte version — bekræftet fx for `profile_reader_agent.py` (STG-003/007/009/011-rettelser), `stage_info_agent.py` (STG-012), `elevation_image_agent.py` (STG-001/008), `climbfinder_agent.py` (STG-004/009)). De 14 rene identiske dubletter blev færdiggjort som `git mv`; de 8 hvor `agents/`-kopien var forældet/forkert navngivet blev overskrevet med rod-versionen og flyttet korrekt ind; alle øvrige ~35 `- Kopi`-filer (verificeret indholdsmæssigt identiske med eller ældre end deres rod-original — ingen tabt arbejde) blev slettet. `daily_update.py`s to `run_script()`-kald til `startlist_agent.py`/`stage_pcs_agent.py` opdateret til `agents/`-stien, da scriptet selv ligger ved roden og ikke selv flyttes. Alle 23 berørte filer verificeret med `python -m py_compile` efter flytning. **Race_prep_pipeline.py's fulde 7-trins afhængighedskæde findes nu korrekt i `agents/`.** |
| OPS-007 | HØJ | LØST | direktør | **Opgraderet fra LAV — reelt ramt produktion i 4 dage, midt i TdF-opstart.** Marketing-agenten opdagede at `daily_pipeline.ps1` (kører dagligt 17:00 via Windows Task Scheduler) har peget på rod-stier for 4 scripts siden mappe-flytningen — verificeret i `logs/pipeline_20260701_1730.log` t.o.m. `_20260703_1730.log`: **RSS-scraping, etaperesultat-scraping, TV-tider og social-posting har ikke kørt automatisk siden 1. juli.** Kun `ai_news_processor.py` (stadig ved roden) og `race_prep_pipeline.py`-relaterede kørsler var upåvirket. Rettet 2026-07-04: (1) verificerede at `agents/results_agent.py`, `rss_news_scraper.py`, `tv_agent.py` er byte-identiske rene flytninger (kun linjeskift-forskel), og at `agents/social_agent.py` kun har den forventede MKT-002-rettelse — alle fire nu `git mv`'et korrekt ind i `agents/` (bevarer historik). (2) `daily_pipeline.ps1` opdateret til at pege på `$ROOT\agents\...` for de 4 scripts. (3) Kørt `results_agent.py`, `rss_news_scraper.py` og `tv_agent.py` manuelt for at indhente det forsømte — etape 1-resultat endnu ikke tilgængeligt på PCS (løbet ikke afsluttet endnu), 30 nye RSS-artikler hentet, 21 TV-programmer gemt. `social_agent.py` ikke kørt manuelt, da Meta-tokenet er udløbet (se MKT-004). Committet og pushet. |
| SEO-002 | MIDDEL | LØST | seo-agent | Sitemap indsendt til GSC (✓). Service account virker og henter rigtige data (testkørt 2026-07-03). |
| MKT-001 | LAV | HENLAGT | ejer | TikTok auto-post uden API for risikabelt (account-ban). Afventer officiel TikTok Content Posting API-adgang. |
| SEO-003 | HØJ | LØST | seo-agent | Canonical URL (`alternates.canonical`) tilføjet 2026-07-03 til alle fem `generateMetadata`-funktioner (race, stage, rider, team, artikel), med bevaret RSS `alternates.types`. Build + typecheck verificeret grønt. |
| SEO-004 | HØJ | LØST | seo-agent | `BreadcrumbList` JSON-LD tilføjet 2026-07-03 på etapesider (`/[slug]/stage/[n]/page.tsx`): Klassementet → løb → etape. Typecheck grønt. |
| SEO-005 | MIDDEL | LØST | seo-agent | Verificeret 2026-07-03: `Person` JSON-LD var allerede implementeret på rytterprofiler (`/riders/[slug]/page.tsx`) i en tidligere kørsel — blot ikke markeret som løst. |
| SEO-006 | HØJ | LØST | seo-agent | Meta-beskrivelser på løbsider (`/[slug]/page.tsx`) opdateret 2026-07-03: inkluderer nu datointerval, etape-antal (nøgleord "etaper" — matcher north star), og gren-specifik tekst for endagsløb vs. etapeløb. Gælder alle løb, inkl. Tour de France. Build + typecheck grønt. |
| SEO-007 | MIDDEL | LØST | seo-agent | IndexNow implementeret 2026-07-03: nøglefil i `cykel-frontend/public/`, `submit_indexnow()` i `api.py` kaldes som baggrundsopgave ved artikel-godkendelse (`/admin/articles/{id}/approve`). Nøglen er ikke hemmelig (IndexNow-protokollen kræver offentlig nøglefil). |
| SEO-008 | MIDDEL | LØST | seo-agent | `agents/gsc_agent.py` bygget og testkørt 2026-07-03 med rigtige Railway-credentials (`GSC_SERVICE_ACCOUNT_JSON_B64` + `GSC_PROPERTY_URL=sc-domain:klassementet.dk`). Fandt reelle data: kun 10 sider med søgeeksponeringer sidste 28 dage, TDF-siden ranker pos. 25.7 med 0% CTR. |
| SEO-009 | HØJ | LØST | direktør | **Soft-404 rettet på alle 6 dynamiske sidetyper** (løb, etape, hold, rytter, artikel, advertorial): manglende indhold returnerede HTTP 200 med "ikke fundet"-tekst i stedet for rigtig 404. Fundet via GSC-rapport: en ugyldig URL (`tour-de-france-løb`, ingen matchende race i DB) var indekseret og konkurrerede mod den rigtige TDF-side. Rettet 2026-07-03 med `notFound()` fra `next/navigation` på alle 6 sider. Build + typecheck grønt. |
| MKT-002 | LAV | LØST | marketing-agent | Rodårsag var ikke navngivningen — `instagram_carousel_daily.py` har aldrig kørt automatisk (rent CLI-script, ingen scheduler). Det reelle problem var `agents/social_agent.py`, som poster ét IG-billede **per artikel, hver dag** (kørt dagligt via `daily_pipeline.ps1` trin 5) — præcis den spam-adfærd der er droppet til fordel for den ugentlige karrusel. Rettet 2026-07-04: per-artikel IG-posting deaktiveret som standard (nyt `--allow-legacy-ig-per-artikel`-nødflag), FB-delen urørt. |
| MKT-003 | MIDDEL | LØST | marketing-agent | Verificeret 2026-07-04: allerede korrekt implementeret i `fb_article_image.py`s `_fb_post()` — opslagstekst uden URL, link postes som separat kommentar. Samme mønster korrekt i `social_agent.py`s `post_facebook()`. Ingen ændring nødvendig. |
| MKT-005 | LAV | LØST | direktør | **Nyt IG-koncept, ejer-godkendt direkte:** ejeren bad om en "vi har alt til Tour de France"-karrusel. Bygget `instagram_tdf_readiness_carousel.py` (genbruger `_base_canvas`/`_add_logo_header`/`slide_branding` fra `instagram_carousel_daily.py`, samme mønster som `instagram_carousel_bignews.py`) — 5 slides: hero ("Alt du skal bruge til touren"), stigningsprofiler, startliste/holdkort, daglige resultater/nyheder, branding-closer. Postet succesfuldt til Instagram 2026-07-04 (post-id `18124238053775445`), link i første kommentar. Committet. |
| MKT-004 | MIDDEL | AFVENTER_EJER | direktør + ejer | Meta Page-tokenet udløb (`OAuthException code 190`), blokerede al social posting midt i TdF-launch. Ejeren kørte `facebook_auth.py`, men den gemte værdi viste sig fejlbehæftet (hele OAuth-redirect-fragmentet blevet gemt som token i stedet for kun selve strengen). Direktøren udtrak den rigtige `long_lived_token`-værdi og udvekslede den, men opdagede at det resulterende token var en **bruger-token** (type `USER`), ikke en **side-token** (type `PAGE`) — Facebook afviste posts med "(#200) publish_actions ikke tilgængelig". Rettet ved at kalde `/me/accounts` direkte og gemme den ægte side-token (bekræftet `type: PAGE`, inkl. `pages_manage_posts`) i `.env`. **Kræver stadig ejeren:** opdatér `META_PAGE_ACCESS_TOKEN` i Railway til den nye, korrekte side-token-værdi (den nuværende Railway-værdi er sandsynligvis stadig den fejlbehæftede/bruger-token). Verificeret løst: TdF data-bekræftet-opslaget blev postet succesfuldt til Facebook 2026-07-04 13:36 med link korrekt i første kommentar. **Opfølgning 2026-07-07 (ejeren rapporterede tvivl om godkendte artikler var "gået tabt"):** direkte databasetjek viser at godkendelses-pipelinen virker korrekt — draft-køen er tom, og alle godkendte artikler (senest etape 3-resultatet, 2026-07-06) er reelt publiceret på klassementet.dk. **Men** direkte test af `META_PAGE_ACCESS_TOKEN` mod Graph API viser at det ER udløbet — allerede samme dag det blev sat (4. juli, ~2 timer efter udstedelse), selvom side-tokens fra `/me/accounts` normalt ikke udløber. Tyder på at et kortlivet token blev gemt i stedet for det langtidsholdbare. Konsekvens: **alle artikler godkendt siden 4. juli er gået korrekt live på siden, men er IKKE postet på Facebook** — `_fb_task()` fejler stille i baggrunden (kun logget til Railway-logs, ingen fejl vist i admin-UI). Direktøren har sendt en ny OAuth-login-URL til ejeren (kan gennemføres fra telefonen) for at hente et nyt token; afventer ejerens svar med redirect-URL'en. **Sidegevinst identificeret, ikke løst endnu:** admin-godkendelse bør overveje at overfladegøre FB-post-status (success/fejl) til ejeren i stedet for at være helt tavs. |
| OPS-001 | MIDDEL | LØST | seo-agent | Forbind connectorer til routinen: Slack (#direktøren — gjort), Search Console (GSC service account testkørt succesfuldt 2026-07-03 med rigtige credentials). |
| OPS-002 | MIDDEL | LØST | ejer | Alle routinemiljøvariabler sat i Railway. Løst 2026-06-28. |
| OPS-003 | MIDDEL | AFVENTER_EJER | ejer | Konfigurér routinens netværks-allowlist (Supabase, climbfinder.com, procyclingstats.com, Nominatim, graph.facebook.com, searchconsole.googleapis.com, pagespeedonline.googleapis.com, api.indexnow.org). |
| OPS-004 | LAV | LØST | direktør | Opgave-dashboard bygget 2026-07-03: `GET /admin/issues` i `api.py` parser denne fil til JSON, vist som Kanban-board på `klassementet.dk/admin/opgaver` (grupperet efter status, farvet efter prioritet, viser ansvarlig). Build + typecheck verificeret grønt. |
| STG-005 | MIDDEL | LØST | direktør | **GPX-baseret stigningsprofil-generator bygget og testkørt 2026-07-03** (`agents/climb_profile_generator.py`) som fallback når ClimbFinder ikke har et verificeret match — genererer klassementet.dk's egne billeder direkte fra rå GPX-højdedata (farvede sektioner, hvid→rød→mørkerød→sort efter hældning). Testkørt for tour-de-france-2026 etape 2: alle 4 stigninger validerede korrekt efter en rettelse undervejs (proportional GPX-segment-lokalisering overskød systematisk, rettet til en vinduessøgning der scorer mod DB-data — se plan-dokumentets afvigelsesnote). Ejeren gennemgik 8 test-billeder og valgte "fuld" stilart samt 20 gradient-sektioner (op fra 10) for et mere præcist/smidigt terrænsilhuet. Skrevet til produktion for etape 2: Côte de Begues og Côte de l'Estadi Olímpic (de to stigninger uden ClimbFinder-billede) har nu `profile_image_url` med `source="generated"`. De to stigninger med eksisterende, verificerede ClimbFinder-billeder er ikke rørt. Se `docs/superpowers/specs/2026-07-03-gpx-climb-profile-generator-design.md` og `docs/superpowers/plans/2026-07-03-gpx-climb-profile-generator.md`. **Opfølgning:** ejeren overvejer at udrulle til alle Tour de France-etaper og evt. omlægge stigningsagenten til at bruge denne generator som primær kilde i stedet for ClimbFinder — afventer en fremtidig sessions-prompt til dette. |
| STG-006 | MIDDEL | LØST | stigningsagent | **AFSLUTTENDE RETTELSE 2026-07-04:** rodårsagen til at etape 6 "manglede GPX-kilde" var en tredje bug — `get_gpx_url_for_stage()`s regex matchede kun `stage-N-route.gpx`, men cyclingstage.com serverer etape 5 og 6 som `stage-N.gpx` (uden "-route"). Rettet regex til at matche begge mønstre, verificeret mod alle 21 etaper (ingen regression, alle 23 unit-tests fortsat grønne). Kørt for etape 6: alle 5 stigninger bestod within_tolerance(), inkl. de to sidste TdF-huller Côte de Loucroup og Gavarnie-Gèdre. **TdF står nu på 71/72 (99%) stigninger med billede** — eneste resterende er Côte d'Engins, se STG-013. Committet `ebe563d`, pushet. **`climb_profile_generator.py` gjort til fast, automatisk pipeline-trin og udrullet til hele tour-de-france-2026 2026-07-03.** Trin: (1) `ARKITEKTUR.md` og `.claude/agents/stigningsagent.md` opdateret — generatoren er nu trin 5 i stignings-pipelinen (efter `climbfinder_agent.py`, før `climb_region_agent.py`). (2) `race_prep_pipeline.py` udvidet fra 6 til 7 trin: nyt trin 7/7 kalder `climb_profile_generator.py --race SLUG --all --style full --write-db` automatisk for ethvert løb — ufarlig for løb uden GPX-kilde (`CYCLINGSTAGE_GPX_PAGES`), da den da bare logger og springer over uden at fejle. (3) Scriptet fik et nyt `--all`-flag (loop over alle etaper i ét løb via `get_stages_for_race()` + refaktoreret `process_one_stage()`), verificeret mod de 23 eksisterende unit-tests (alle grønne). (4) Kørt `--all --write-db` for tour-de-france-2026: af 67 stigninger på tværs af 21 etaper havde **58 allerede et verificeret billede** (mest ClimbFinder + de 4 fra STG-005). Af de resterende 9 uden billede blev **kun Côte de Montjuic (etape 1) succesfuldt genereret og skrevet** (visuelt stikprøvetjekket — korrekt terrænprofil, 52→111m, 5.4% snit). De **8 øvrige fejlede `within_tolerance()`** og blev korrekt sprunget over/logget i stedet for gættet på: Les Angles (etape 3, 95m vs DB 1793m), Puy Boubou (etape 9, 172m vs DB 290m), Côte de Miel (etape 9, 257m vs DB 525m), Mont Bessou (etape 9, 58m vs DB 977m), Côte de Pailherols (etape 10, 325m vs DB 1042m), Côte de Larringes (etape 16, 402m vs DB 795m), Orcières-Merlette (etape 18, 476m vs DB 1820m), Barrage de Grand'Maison (etape 20, 535m vs DB 1720m) — alle undervurderer højdemeter markant, hvilket tyder på at vinduessøgningen (±25 km) ikke rammer hele det rigtige GPX-segment på lange, klatretunge etaper med flere stigninger i træk (samme driftsproblem som STG-005 beskrev, blot mere udtalt her). **Yderligere fund:** etape 6 har ingen GPX-fil tilgængelig på cyclingstage.com overhovedet (`get_gpx_url_for_stage()` returnerer `None`) — af etapens 5 stigninger mangler **Gavarnie-Gèdre** fortsat billede og kunne slet ikke forsøges. 7 etaper har ingen stigninger (5, 7, 8, 11, 12, 17, 21). **Ingen eksisterende ClimbFinder-billeder rørt.** **Opfølgning:** de 9 stadig-manglende stigninger (8 tolerance-fejl + Gavarnie-Gèdre) kræver enten en bredere/smartere segment-lokalisering (større søgeradius eller drift-kompensation for etaper med mange stigninger) eller en alternativ GPX-kilde for etape 6, før de kan få et billede uden at gætte. **RETTELSE (samme dag):** diagnosen om utilstrækkelig søgeradius var forkert — se STG-007. Rodårsagen var forkert `elevation_m`-data (tophøjde i stedet for højdemeter). Efter rettelsen bestod alle 8 climbs `within_tolerance()` uden nogen ændring af søgealgoritmen; 7 af dem nu skrevet til DB. |
| STG-013 | MIDDEL | LØST | direktør | **Côte d'Engins (etape 18) — sidste manglende TdF-stigning, nu lukket.** Ejeren godkendte 2026-07-04 rettelsen af `elevation_m` fra 855 (tophøjde, fejlagtigt gemt som gain — samme STG-007-bugklasse) til 638m (længde×hældning, bekræftet af uafhængig GPX-terræn ≈639m). Direktøren skrev rettelsen direkte til Supabase (verificeret før/efter), og kørte derefter `climb_profile_generator.py --race tour-de-france-2026 --stage 18 --write-db`: stigningen bestod nu `within_tolerance()` og fik et rigtigt GPX-verificeret billede. **Tour de France 2026 står nu på 88/88 (100%) stigninger med verificeret billede.** |
| STG-007 | HØJ | LØST | direktør | **Fandt og rettede fundamental data-bug: `stage_climbs.elevation_m` indeholdt for de fleste rækker stigningens tophøjde (meter over havet) i stedet for højdemeter klatret (gain)** — modsat hvad feltet bruges til alle andre steder (frontend viser "Højdemeter: +Xm", climbfinder/climb-generator sammenligner det som gain). Rodårsag: `profile_reader_agent.py` linje ~311-319 skrev Claude-visions `altitude_m` (dokumenteret i VISION_PROMPT som "summit altitude") direkte ind i `elevation_m`, når Claude kunne aflæse en tophøjde på billedet — kun når den ikke kunne, blev der beregnet en (korrekt) gain via `length_km × avg_gradient × 10`. Audit af hele databasen: **118 af 129 rækker (91%) ramt**, ikke kun Tour de France (også Giro- og Tour de Suisse-stigninger). Eksempler: Col du Galibier viste 2631m (reel tophøjde ~2642m) i stedet for reel gain ~1190m; Mont Bessou viste 977m (reel tophøjde) i stedet for ~68m gain. Rettet 2026-07-03: (1) `profile_reader_agent.py` byttet prioritet — beregner nu altid gain fra length/gradient når muligt, bruger kun rå tophøjde som sidste udvej (med synlig advarsel). (2) Nyt script `agents/fix_elevation_gain_backfill.py` (dry-run som default, `--write` for at skrive) genberegnede og skrev alle 118 rammede rækker — 0 fejlede, 0 mistænkelige rækker tilbage ved efterfølgende audit. Ingen andre felter rørt. **Konsekvens:** dette var også rodårsagen til de "8 manglende stigninger" i STG-006 (se rettelse ovenfor) og gjorde `climbfinder_agent.py`s strammede `metrics_ok()`-tolerancer (STG-004) upålidelige for høje bjerge, fordi de nogle gange sammenlignede reel gain mod ClimbFinders tophøjde. **Opfølgning — TO efterfølgende selv-forårsagede regressioner fundet og rettet samme dag, før produktion stabiliserede sig:** (A) Første fulde re-verifikationskørsel (`--all --overwrite`) sammenlignede stadig CF's ægte `finishElevation` (tophøjde) direkte mod vores nu-korrekte `elevation_m` (gain) — to forskellige fysiske størrelser, som næsten aldrig matcher for rigtige bjerge. Resultat: **105 tidligere-korrekte ClimbFinder-matches ryddet, kun 23 nye godkendt** (bl.a. Col du Tourmalet og Col d'Aspin — begge reelt korrekte match — blev fejlagtigt slettet). Rettet ved at lade `metrics_ok()` udlede CF's egen implicerede gain fra dens length×gradient (samme formel som vores DB) i stedet for at bruge CF's tophøjde-felt. Genkørt: 76 matches gemt, kun 4 yderligere ryddet — sund ratio. (B) Denne genkørsel afslørede et ANDET problem: `--overwrite` rørte også stigninger markeret `SEARCH_OVERRIDES[navn] = None` (kendt IKKE på ClimbFinder — netop dem `climb_profile_generator.py` laver GPX-fallback-billeder til), og ryddede ubetinget deres eksisterende billede hver gang ingen CF-match kunne findes (hvilket er garanteret for disse). Alle 11 GPX-genererede fallback-billeder fra STG-006 blev derved slettet igen, TO gange. DB-dækning faldt fra oprindelige 130 til kun **76** klatringer med billede — værre end før dagens arbejde. Rettet i `climbfinder_agent.py`: stigninger med `SEARCH_OVERRIDES[navn] = None` springes nu helt over uden at røre et eksisterende billede. Genkørte `climb_profile_generator.py --all --write-db` for alle 4 løb for at retablere de slettede fallback-billeder. **Slutresultat samme dag:** DB-dækning genoprettet til 115/149 klatringer med billede; **Tour de France specifikt: 69/72 (96%)** — kun Côte de Loucroup + Gavarnie-Gèdre (etape 6, ingen GPX-kilde) og Côte d'Engins (etape 18, fejler `within_tolerance()`) mangler stadig. Giro/Dauphiné/Tour de Suisse har lavere dækning pga. manglende GPX-sider på cyclingstage.com for de fleste af deres etaper — lavere prioritet, da de ikke kører nu. **Læring:** enhver ændring af `elevation_m`-semantik eller af hvornår `--overwrite` rører eksisterende billeder skal fremover testes på étt løb først og eksplicit tjekkes for at den ikke rammer `SEARCH_OVERRIDES=None`-stigninger, før den køres mod hele databasen. |
| OPS-006 | LAV | LØST | direktør | **Løst via Telegram-integration (2026-07-06).** Telegram-plugin'et er sat op og parret (bot-token konfigureret, ejerens bruger-ID allerlisted i `~/.claude/channels/telegram/access.json`) — Claude Code-sessioner startet med `claude --channels plugin:telegram@claude-plugins-official` kan nu sende/modtage beskeder direkte til ejerens telefon via Telegram-appen, verificeret med en test-besked frem og tilbage. En PowerShell-profilfunktion (`Microsoft.PowerShell_profile.ps1`) blev tilføjet, så almindeligt `claude` i terminalen automatisk inkluderer channel-flaget, uden ejeren skal huske det. Dette dækker det oprindelige behov (gratis push-besked ved session-afslutning) og bruges nu desuden som kanal for den daglige optimerings-digest (se OPS-009). |
| STG-008 | MIDDEL | LØST | direktør | **Etape 6 (tour-de-france-2026, Gavarnie-Gèdre) viste en enkelt stignings profilbillede (Côte de Loucroup) i stedet for hele etapens højdeprofil.** Rodårsag fundet ved live-undersøgelse af PCS' `/info/profiles`-side via Playwright: for etaper med en fremtrædende (HC-)stigning leverer PCS to "profile"-billeder, ikke ét — her `profile-n2` (Côte de Loucroups eget zoomede diagram, 34 KB) og `profile-n3` (den ægte fulde etapeprofil med alle stigninger markeret, 107 KB). `elevation_image_agent.py`s "mindste vinder"-heuristik (fra STG-001, som skulle undgå rutekort) valgte fejlagtigt den mindste — n2 — i stedet for den rigtige. Stikprøve på etape 2 (som viser korrekt i dag) bekræftede: den har kun ét "profile"-kandidatbillede, så heuristikken var uskadelig der. Rettet 2026-07-04: `capture_sprint_profile()` vælger nu den STØRSTE kandidat i stedet for den mindste (rutekort er allerede udelukket separat via filnavn `map` og `MAX_BYTES`). Etape 6 genhentet og bekræftet visuelt korrekt (Pau → Gavarnie-Gèdre, alle 4 stigninger inkl. Col du Tourmalet markeret). Ikke afprøvet mod resten af databasen — bør stikprøvetjekkes på andre fler-profil-etaper (typisk stadier med HC-stigninger) ved lejlighed. |
| RES-001 | MIDDEL | LØST | direktør | **Etape-siden viser nu to fold-ud-paneler side om side: etaperesultat og klassementet (GC/point/bjerge/ungdom) som det så ud lige efter den etape** — bygget 2026-07-05, kun for tour-de-france-2026 indtil videre. Nyt API-endpoint `GET /races/{slug}/stages/{n}/classifications/{type}` (erstatter det gamle GC-kun `/gc`-endpoint) i `api.py`. Nye frontend-komponenter `Disclosure.tsx` og `ClassificationTabs.tsx` (udtrukket fra `SpoilerSection.tsx`, som nu genbruger den). **Fandt og rettede undervejs en reel bug i `agents/results_agent.py`:** scraperen brugte faste PCS-tabel-indekser (0=etape, 1=GC, 2=point, 6=bjerge, 7=ungdom), som gik i stykker for etape 1 (holdenkeltstart) — PCS' side har 23 per-hold rytterliste-widgets FØR de rigtige tabeller, hvilket forskød alle indekser og fik scraperen til stille at finde tomme tabeller. Rettet til at finde tabellerne via indhold (`td.ridername` + min. 5 rækker inde i `#resultsCont`, parret med nærmeste fane-overskrift som "Youth day classification") i stedet for position. Kørt for etape 1: 10 etaperesultater + 20 GC + 20 ungdomsklassement-rækker skrevet korrekt til produktionsdatabasen og verificeret live på `klassementet.dk/tour-de-france-2026/stage/1`. Point og bjerge er korrekt tomme for denne etape — en holdenkeltstart uddeler ingen sprint- eller KOM-point, så PCS' side har slet ingen faneblade for dem endnu (ikke en fejl). **Opfølgning:** `gc_table`- og `points`/`mountains`-identifikationen (matchet på overskriftstekst "gc"/"point"/"mountain"/"kom") er kun verificeret mod denne ene holdenkeltstart-side — bør stikprøvetjekkes igen når etape 2 (en almindelig linjeetape) er kørt, for at bekræfte at en separat GC-fane (adskilt fra etaperesultatet) identificeres korrekt, og at et evt. reelt bjergklassement (fx hvis nogen fører med 0 point pga. tidskvalifikation) fanges rigtigt. |
| RES-002 | HØJ | LØST | direktør | **Rettede fejlagtig "+0:00" for ryttere langt nede i feltet i GC/ungdomsklassement.** Ejeren opdagede at etape-siden viste "+0:00" (tid til lederen) for ryttere, der reelt lå adskillige sekunder/minutter efter — rodårsag: PCS udelader (tom celle eller "0:00") tidsfeltet for en rytter med samme tid som rytteren *lige før* i ranglisten (for at undgå at gentage identiske værdier), men `results_agent.py` tolkede et tomt/uparsérbart tidsfelt som "0 sekunder til lederen" for alle rytter, ikke kun den første i hver tidsgruppe. Rettet med ny `_resolve_gap()`-hjælpefunktion, der viderefører **forrige rytters gap** i stedet for at antage 0, brugt i etaperesultat-, GC- og ungdomsklassement-udtrækket. **Fandt samtidig endnu en bug:** ungdomsklassementets tidskolonne blev læst via et fast indeks (`tds[9]`), som er uden for rækkevidde på den 9-kolonners ungdomstabel (ingen "UCI"-kolonne som GC-tabellen har) — alle ungdomsrytteres gap blev derfor altid skrevet som 0, uanset carry-forward-rettelsen. Rettet til samme klasse-baserede opslag (`td.time`) som GC allerede bruger. Genkørt for tour-de-france-2026 etape 1 og verificeret direkte i databasen: GC og ungdom viser nu korrekte, forskellige gap-værdier gennem hele feltet (fx to ryttere korrekt tied på 39 sek i GC, ingen falske nulstillinger). Points-/bjergklassementernes `tds[9]`-pointudtræk er **ikke** rørt — uverificeret mod en rigtig side (ingen sådan tabel fandtes for etape 1's holdenkeltstart) og bør tjekkes separat, hvis/når det bliver relevant. |
| SEO-011 | HØJ | LØST | direktør | **Loft på max 2 artikler/dag (mål 1) — for mange kladder skader både SEO og brandet.** Ejeren opdagede ca. 10 artikelkladder i admin-køen fra én dag — for mange, både ift. SEO-strategien mens siden venter på Google-indeksering (mange tynde artikler frem for få stærke) og brandets "kun det vigtige"-løfte (`CLAUDE.md` §5). Rodårsag: `ai_news_processor.py` havde intet loft på antal SKREVNE artikler — den scorede op til 15 kandidater og skrev én kladde for **hver eneste**, der scorede ≥8/10, uden at sammenligne dem mod hinanden. Rettet: ny `MAX_DRAFTS_PER_RUN` (default 2, mål 1) — scriptet scorer nu alle kandidater først, rangerer de kvalificerede efter relevans, og skriver kun den/de allerbedste; resten markeres behandlet uden at blive skrevet (undgår gentagelse i morgen). Nyt `--max-drafts`-flag. `STRATEGI.md` §1 og `.claude/agents/artikel-agent.md` opdateret til samme politik. Committet, pushet. **Ikke gjort:** de ~10 eksisterende kladder i admin-køen er urørt — det er ejerens egen godkendelses-/afvisningsbeslutning, ikke ryddet automatisk. |
| STG-015 | MIDDEL | LØST | direktør | **Etape 3 (tour-de-france-2026) viste vores egen genererede hel-etape-højdeprofil i stedet for PCS' billede.** Baggrund (se STG-002): `stages.elevation_image_url` blev sat 2026-07-04 af `agents/stage_profile_generator.py` som fallback pga. ægte PCS-datamangel på det tidspunkt. **Rettet 2026-07-06:** PCS havde nu publiceret det rigtige hel-etape-profilbillede (Granollers–Les Angles, alle klatringer inkl. Col de Toses og Col du Calvaire), men under et usædvanligt filnavnsmønster (`...-final-km-<hash>.jpg` i stedet for det sædvanlige `-profile-`/`-sprint-`) — derfor havde `elevation_image_agent.py`s normale filnavns-heuristik (og dens filter, der kun genbehandler etaper hvis EKSISTERENDE billede allerede er en PCS-URL) aldrig fanget det automatisk. Fundet ved manuel Playwright-gennemgang af `/info/profiles`-siden og verificeret visuelt (skærmbillede bekræftede ægte terrænprofil med korrekte km-markører og stigninger). Billedet downloadet og uploadet til Supabase Storage under samme `{race_id}/{filnavn}`-konvention som de øvrige etaper, og `elevation_image_url` overskrevet. Verificeret i DB og at storage-objektet server det korrekte billede (1250×625). Frontend viser det automatisk inden for `revalidate=300`s ISR-vinduet — ikke rørt yderligere. **Opfølgning:** ingen — `elevation_image_agent.py` er bevidst ikke ændret, da dette var et engangs-filnavnsafvig for denne ene etape, ikke et generelt mønster (endnu). |
| SEO-010 | MIDDEL | LØST | direktør | **IndexNow udvidet til løbs-/etapesider, ikke kun artikelgodkendelse.** Allerede implementeret og committet (`dae7764`, "feat(SEO-010): trig IndexNow ved loebs-/etapeopdatering") men aldrig logget her, og `ARKITEKTUR.md` beskrev fortsat kun artikel-triggeret IndexNow. `daily_update.py` har en `notify_indexnow()`-funktion, der melder løbets side + samtlige etapesider til IndexNow, hver gang startliste eller etapedata oprettes/opdateres for løbet i en kørsel. Retroaktivt logget og `ARKITEKTUR.md` opdateret 2026-07-06, efter ejeren bad om præcis denne udvidelse (uden at vide den allerede fandtes). **Vigtig afgrænsning:** Google understøtter ikke IndexNow-protokollen (kun Bing/Yandex/Naver/Seznam/Yep) — det hjælper altså ikke direkte på Google-indekseringsproblemet (GSC-fund samme dag: kun forside + `/tour-de-france-2026` er reelt "Submitted and indexed", alle etape-/rytter-/hold-/nyhedssider er enten "URL is unknown to Google" eller "Discovered - currently not indexed"). **Opfølgning:** kun ryttere og hold mangler IndexNow-dækning nu — kan tilføjes samme mønster i deres respektive opdateringsscripts, hvis ønsket. Google-indekseringen selv løses ikke af IndexNow, men af sitemap + intern linking + tid/autoritet (jf. STRATEGI.md §1). |
| OPS-009 | MIDDEL | LØST | direktør | **Ny adfærd: "gå i gang med at arbejde" uden kontekst udløser nu et struktureret optimerings-scan i stedet for at direktøren selv vælger en opgave.** Design: `docs/superpowers/specs/2026-07-07-daglig-optimeringsscan-design.md`. Kontrolagenten (nu med Playwright-skærmbillede-adgang) finder både bugs og nye optimeringspunkter (design, UX, marketing-kvalitet/kvantitet); bugs auto-tildeles som hidtil, men optimeringsforslag logges altid som `NY`/"Forslag (afventer godkendelse):" og kræver ejerens eksplicitte ja, før nogen agent går i gang — aldrig automatisk `TILDELT`. Relevante forslag sendes til research-agenten med en afgrænset, konkret brief pr. punkt (ikke bred fritekst-research). `PROTOKOL.md` og `ORKESTRATOR.md` opdateret: Slack `#direktøren` erstattet af Telegram overalt (jf. OPS-006), og en generel regel tilføjet om at sende Telegram-besked, så snart noget kræver ejerens stillingtagen. Ingen automatisk tidspunkt/cron — ejeren kunne ikke oprette en Windows Task Scheduler-opgave, så scannet udløses udelukkende af ejerens egen "gå i gang"-instruktion. |
| OPS-010 | HØJ | LØST | direktør | **Fandt under dagens optimeringsscan (kontrolagent): forsiden og TdF-løbssiden viste gårsdagens etape som "i dag", midt i touren.** Rodårsag: `api.py`s `/ongoing-races` og `/upcoming-races` brugte `date.today()`, som følger serverens (UTC) lokale dato — mellem midnat og kl. 02 dansk sommertid (UTC+2) er UTC-datoen stadig "i går", så den netop afsluttede etape blev vist som dagens, og etape-tælleren ("X/21 afsluttet") var 1 for lav. Rettet 2026-07-07: ny `today_dk()`-helper (`zoneinfo.ZoneInfo("Europe/Copenhagen")`) erstatter begge `date.today()`-kald. `python -m py_compile api.py` grønt. **Ikke undersøgt:** øvrige scripts i `agents/` kan have samme mønster (`date.today()` uden tidszone) — ikke auditeret i denne omgang, da kontrolagentens fund kun pegede på disse to endpoints. |
| RES-003 | HØJ | LØST | direktør | **Fandt under dagens optimeringsscan (kontrolagent): rytterprofiler viste en igangværende Tour de France 2026 som allerede vundet.** Pogačar og Vingegaard fremstod med en TdF 2026-GC-placering i "palmares" (`/riders/{slug}/palmares`), selvom løbet kun var kørt 3-4 af 21 etaper. Rodårsag: `api.py`s `get_rider_palmares()` filtrerede `classifications`-tabellen på `classification_type=eq.gc&position=lte.10` uden at tjekke om løbet reelt var afsluttet — en igangværende etapes mellemstilling blev altså vist som en permanent sejr/placering, i strid med `CLAUDE.md` §4/§6. Rettet 2026-07-07: tilføjet filter så kun løb med `end_date < i dag` (dansk tid, via samme `today_dk()`) tæller med i `gc_results`. Etapesejre (`stage_wins`) er ikke rørt, da en enkelt etapesejr er et reelt, afsluttet resultat med det samme. `python -m py_compile api.py` grønt. Rammer alle ryttere i alle igangværende løb, ikke kun TdF — ingen backfill nødvendig, da dette er et visningsfilter på læsetidspunktet, ikke en datafejl i selve tabellen. |
| SEO-013 | HØJ | LØST | direktør | **Opfølgning til SEO-001: legacy-redirect for `tour-de-france-løb` virkede ikke live trods commit `58d0855` (2026-07-04).** Fundet af seo-agenten under dagens GSC-kontrol (bekræftet 404 i stedet for redirect, 3 gentagne kald). Rodårsag fundet ved lokal debugging (`next dev` + midlertidig logging, ikke i produktion): i denne Next.js-version (16.2.4, jf. `cykel-frontend/AGENTS.md`s advarsel om breaking changes) leverer `params.slug` det **rå, procent-kodede** segment (`"tour-de-france-l%C3%B8b"`) i stedet for at afkode det selv som i tidligere Next.js-versioner — derfor matchede `LEGACY_RACE_NAME_ALIASES[slug]`-opslaget (nøgle med rigtigt "ø"-tegn) aldrig. Rettet 2026-07-07: `cykel-frontend/app/[slug]/page.tsx` afkoder nu eksplicit `slug` med `decodeURIComponent()` (try/catch, falder tilbage til rå streng ved ugyldig sekvens) i både `RacePage` og `generateMetadata`, før alias-opslaget. Verificeret lokalt: `/tour-de-france-l%C3%B8b` giver nu `308 Permanent Redirect` → `/tour-de-france-2026`. `npx tsc --noEmit` + `next build` grønt. **Ikke undersøgt:** om andre dynamiske ruter (`riders/[slug]`, `teams/[slug]` osv.) rammes af samme rå-encoding-ændring — sandsynligvis uden praktisk effekt der, da alle nuværende rytter-/hold-/løbs-slugs er rene ASCII uden procent-tegn, men værd at holde in mente ved fremtidige non-ASCII-slugs. |
| SEO-014 | MIDDEL | LØST | direktør | **/nyheder havde forsidens generiske sidetitel ("Klassementet — Dansk cykelportal") i stedet for sin egen — konkret indekseringsbarriere fundet af kontrolagenten.** Rodårsag: `cykel-frontend/app/nyheder/page.tsx` havde slet ingen `metadata`-export, modsat `[slug]`-undersiden som allerede har korrekt `generateMetadata`. Rettet 2026-07-07: tilføjet `export const metadata` med egen titel ("Nyheder"), beskrivelse og `canonical`/RSS-alternates, samme mønster som artikel-siden. `npx tsc --noEmit` + `next build` grønt. |
| MKT-006 | MIDDEL | LØST | direktør | **Holdbeskrivelse med tekstfejl fundet af kontrolagenten: "UAE Team Emirates - XRG er etWorldTour-hold..." (manglende nationalitetsord + mellemrum).** Rodårsag: `teams.country_code` er `NULL` for dette hold, hvilket fik `team_info_agent.py`s prompt til at sende `(?)` som landekode, og AI'en genererede en ufuldstændig sætning som konsekvens. **Rettet 2026-07-07 (som del af OPT-004):** prompten udelader nu landekoden helt når den mangler, i stedet for at sende `(?)`. `country_code` er bevidst ikke backfillet (kræver en rigtig kilde, ikke en gættet værdi — jf. `CLAUDE.md` §6), men beskrivelsen omtaler nu holdet uden at påstå en nationalitet, hvilket er korrekt givet manglende data. Regenereret og verificeret: "UAE Team Emirates-XRG er et tophold med stærke klassiker-ryttere og etapekørere, anført af stjerner som Tadej Pogačar og Juan Ayuso." — ingen sprogfejl. |
| MKT-007 | LAV | LØST | direktør | **AI-hallucination i etape 6-beskrivelsen: "dansk mountainbike-talent" i en landevejsetape.** Fundet af kontrolagenten under dagens anden kritiske gennemgang — samme fejlklasse som MKT-006/OPT-004 (AI-genereret tekst med en faktuel fejl), her i en etapebeskrivelse i stedet for en holdtekst. Rettet 2026-07-07: regenereret via `agents/stage_info_agent.py --race tour-de-france-2026 --stage 6` (samme, allerede STG-012-præcisionsrettede prompt). Verificeret live på klassementet.dk: ny tekst nævner ikke mountainbike og er faktuelt korrekt. **Opfølgning:** ikke stikprøvetjekket om lignende hallucinationer findes i andre etapebeskrivelser — værd at holde øje med som en tilbagevendende fejlklasse i AI-genereret tekst. |
| STG-016 | MIDDEL | TILDELT | stigningsagent | **Etape 4 (tour-de-france-2026): forkert gradient på "Col de Villerouge" — fundet af kontrolagenten, krydstjekket direkte mod PCS.** Klassementet.dk viser "8,6 km - 4,6%", men PCS' egen etapeprofil viser samme klatring som "8,6 km - 3%" — 4,6% hører til en anden, nærtstående top på samme PCS-graf ("Col des Fours", som slet ikke optræder på klassementet.dk). Ligner en vision-baseret sammenblanding af to etiketter på PCS-profilbilledet i `profile_reader_agent.py`s udtræk for denne etape. Tildelt stigningsagenten til verifikation og rettelse (evt. tilføj Col des Fours som selvstændig climb, eller ret blot gradienten for Villerouge til 3%). |
| OPS-011 | HØJ | AFVENTER_EJER | direktør + ejer | **Rytter-/holdfotos loader ikke sitewide — HTTP 402 fra Next.js' billedoptimerings-endpoint (`/_next/image`).** Fundet af kontrolagenten: samtlige 30 rytterfotos på `klassementet.dk/teams/uae-team-emirates-xrg` fejler identisk, samt flere fotos på etape 4-siden. HTTP 402 (Payment Required) fra Vercels image-optimizer peger stærkt på en opbrugt kvote/faktureringsbegrænsning for billedtransformationer, ikke en kode- eller datafejl — kan ikke rettes fra denne session. **Kræver ejeren:** tjek Vercel-dashboardets "Image Optimization"-forbrug/faktureringsside med det samme; dette rammer sidebredt i højeste trafikperiode (midt i TdF). **Opfølgning 2026-07-07 (kontrolagentens anden kørsel samme dag):** uafhængigt reconfirmet på etape 4-6-siderne — 13-22 identiske 402-konsolfejl pr. side, mønsteret er uændret. Stadig uløst, stadig kræver ejerens Vercel-dashboard-tjek. |
| OPT-001 | LAV | LØST | direktør | **Ejer-godkendt 2026-07-07.** Ny klientkomponent `CollapsibleList` på rytterprofiler: GC-palmares og etapesejre viser nu de første 8 med en "vis alle (N)"-knap, i stedet for en flad liste (Pogačars side var ~6.800 px). `npx tsc --noEmit` grønt. |
| OPT-002 | MIDDEL | HENLAGT | direktør | **Undersøgt 2026-07-07 — kunne ikke reproduceres.** Testede friske (ikke-klient-navigerede) server-renderede requests direkte mod produktion for både etape 4-siden og en rytterside: begge preloader korrekt deres EGET billede, ikke etape 3's. Sandsynlig forklaring: kontrolagentens Playwright-session navigerede client-side mellem siderne, hvilket kan lade gamle `<link rel=preload>`-tags fra en tidligere besøgt side hænge ved i DOM'en — ikke en reel server-side bug. Kan også hænge sammen med at "i dag"-etapen fejlagtigt var etape 3 før OPS-010-fixet. Ingen kodeændring foretaget, da der ikke er noget at rette. |
| OPT-003 | LAV | LØST | direktør | **Ejer opdagede samtidig en beslægtet, mere akut bekymring (godkendte artikler så ikke ud til at være der) — se opfølgning i MKT-004.** Bekræftet: intet er tabt, /news-API'et returnerer korrekt alle artikler nyeste-først. Det oprindelige fund (ingen adgang til artikler ældre end 24. maj) var reelt en manglende pagination på `/nyheder`. Tilføjet "Indlæs flere"-knap (client-side, genbruger backendens eksisterende `limit`/`offset`-parametre) så ældre nyheder nu er tilgængelige. |
| OPT-004 | LAV | LØST | direktør | **Ejer-godkendt 2026-07-07.** Nyt selv-review-korrekturtrin i `team_info_agent.py`: en ekstra Claude-kald retter sprogfejl (manglende bøjning/mellemrum) i de genererede hold-tekster før DB-skrivning; fejler trinnet, bruges ukorrigerede tekster (aldrig blokerende). Kørt for team-visma-lease-a-bike: rettede "et af verden mest succesfulde hold" → "et af verdens mest succesfulde hold". Samme prompt-rettelse som MKT-006. |
| OPT-004 | LAV | LØST | direktør | **Bogføringsdublet ryddet 2026-07-07:** denne række var det oprindelige forslag ("Forslag (afventer godkendelse)") for Team Visma-korrekturfejlen. Ejeren godkendte og direktøren implementerede det allerede under samme ID (se rækken umiddelbart ovenfor) — denne række blev ved en fejl aldrig opdateret til `LØST` i samme ombæring. Ingen ny handling krævet; slået sammen med raden ovenfor. |
| SEO-015 | MIDDEL | LØST | direktør | **`/races` manglede helt fra `cykel-frontend/app/sitemap.ts`s statiske array** — fundet af seo-agenten under dagens opfølgende GSC-kontrol (2026-07-07). Kun `/riders`, `/teams`, `/nyheder` og forsiden var med; `/races` (løbsoversigten) var ikke, hvilket sandsynligvis medvirker til at GSC fortsat viser siden som "unknown to Google". Rettet: tilføjet samme mønster (`changeFrequency: "daily"`, `priority: 0.6`). `npx tsc --noEmit` grønt. Committet `45ca8c3`. |
| SEO-016 | HØJ | AFVENTER_EJER | ejer | **Den døde legacy-URL `tour-de-france-løb` (SEO-013s redirect-fix) står stadig registreret som "Submitted and indexed" i Google med sidste crawl 2026-06-17 — FØR redirect-rettelsen gik live.** Seo-agenten fandt ved at grave i de rå GSC-query-rækker, at denne døde URL i dag fanger langt størstedelen af vores "tour de france klassement/stilling"-søgetrafik: 26 af 51 søgeordskombinationer og ~211 af ~350 samlede visninger (28 dage), inkl. brede, værdifulde termer som "tour de france klassement" (67 visn., pos 25), "tour de france stilling" (58 visn., pos 29) — termer den RIGTIGE side slet ikke viser sig for i dag. Alt 0% CTR, fordi URL'en teknisk set nu redirecter (SEO-013), men Google ikke har genbesøgt og opdateret sit index endnu. Hverken GSC's offentlige API eller IndexNow (Google understøtter ikke protokollen) kan trigge en re-crawl programmatisk. **Kræver ejeren:** klik "Anmod om indeksering" i Search Console-UI'en for `https://klassementet.dk/tour-de-france-l%C3%B8b`, så Google genbesøger, ser 308-redirecten til `/tour-de-france-2026`, og forhåbentlig konsoliderer den oparbejdede relevans dertil. Tidsfølsomt — værdifuld søgeordsrelevans risikerer at gå tabt jo længere URL'en fejlagtigt topper i indexet. |
| SEO-018 | MIDDEL | LØST | direktør | **Manglende sidespecifik metadata på `/riders`, `/teams` og `/kontakt`** — fundet af kontrolagenten under dagens anden kritiske gennemgang (samme fejlklasse som SEO-014): alle tre viste forsidens generiske titel/beskrivelse. Rettet 2026-07-07: tilføjet `export const metadata` direkte i `riders/page.tsx` og `teams/page.tsx` (server components); `kontakt/page.tsx` er en client component og kan ikke selv eksportere metadata, så nyt `kontakt/layout.tsx` (server component) blev tilføjet til formålet. `npx tsc --noEmit` + `next build` grønt. Committet `4bbd61c`. |
| OPS-012 | HØJ | AFVENTER_EJER | ejer | **Footerens Facebook-link peger på en fremmed persons profil, ikke Klassementets side.** Fundet af kontrolagenten (uafhængig gennemgang, verificeret både via Playwright og WebFetch): linket i `cykel-frontend/app/layout.tsx:69` (`https://www.facebook.com/profile.php?id=61576574048561`) resolver til profilen "Sagriu Ado" — en helt urelateret privatperson. Instagram-linket er til sammenligning korrekt (`instagram.com/klassementet`). Direkte tillids-/brandskade (jf. `CLAUDE.md` §5) og rammer marketing-sporet i `STRATEGI.md` §2. Direktøren kunne ikke finde Klassementets korrekte Facebook-side-ID i kodebasen eller uden at læse hemmeligheder — kræver derfor ejerens bekræftelse af den rigtige side-URL, før linket rettes (jf. `CLAUDE.md` §6: publicér ikke uverificeret data). Muligvis relateret til det udløbne Meta-token i MKT-004, men er et separat, hardkodet frontend-link, ikke en API-nøgle. |
| OPS-013 | HØJ | AFVENTER_EJER | ejer | **Kontaktformularen (`/kontakt`) sender intet nogen steder — den er en fake "coming soon"-attrap, der viser falsk succes.** Fundet af kontrolagenten: `handleSubmit()` i `cykel-frontend/app/kontakt/page.tsx` venter blot 800ms og viser "Tak for din besked!", uden at sende data til nogen backend, e-mail eller database (koden kommenterer selv "Contact form backend activated at domain launch... For now, show a coming soon success state"). Domænet er for længst lanceret og kører live TdF-trafik. **Dette rammer direkte forretningsmodellen** (`CLAUDE.md` §2): siden er indgangen til advertorial-/annonce-henvendelser, og en potentiel kunde, der udfylder formularen, får at vide beskeden er sendt — men den går tabt. **Kræver ejerens beslutning, ikke en autonom kodefix:** jf. tidligere lært regel om at nye løsningsmekanismer skal godkendes af ejeren først — at koble formularen til en reel backend kræver enten (a) et nyt tredjepartsværktøj (fx Formspree, nævnt i kodekommentaren) med tilhørende konto/nøgle, eller (b) et nyt endpoint i `api.py` der sender e-mail via en SMTP-/mailtjeneste, hvilket også kræver nye credentials. Direktøren afventer ejerens valg af metode, før noget bygges. |
| STG-017 | HØJ | TILDELT | stigningsagent | **Etape 4 (Carcassonne–Foix, dagens etape): forkert gradient på Col de Montségur.** Fundet af kontrolagenten: klassementet.dk viser "6.9 km - 6.1%", men tre uafhængige eksterne kilder (cyclingstage.com, inrng.com, francetvinfo/letour.fr) er enige om **6.6%** for stigningens 6,9 km. Sidste og hårdeste kategoriserede top (34,5 km fra mål) på en LIVE etape i dag. Separat, frisk fund fra det allerede tildelte STG-016 (Col de Villerouge, samme etape). Bemærk: topmøde-højden (1047m) nævnes modstridende af én ekstern kilde (~900m) — bør verificeres mod GPX/PCS-rådata, ikke afgøres af sekundære websøgninger. Tildelt sammen med STG-016. |
| STG-018 | LAV | TILDELT | stigningsagent | **Etape 6 (Pau–Gavarnie-Gèdre): mulig gradient-afvigelse på Côte de Mauvezin — lav sikkerhed, markeret til review.** Kontrolagenten fandt at klassementet.dk viser "3 km - 6.4%" mod én ekstern kildes 6.8%; øvrige 4 klatre på samme etape stemte til gengæld præcist overens med kilderne, så dette er et isoleret, lavere-tillid-fund (i modsætning til STG-017). Stigningsagenten bør dobbelttjekke mod GPX/PCS-kildedata, men det er ikke bekræftet en reel fejl endnu. |
| OPT-005 | LAV | NY | — | **Forslag (afventer godkendelse):** klatre-fanerne over etapers højdeprofil følger ikke altid rute-rækkefølgen. Fx etape 4 viser rækkefølgen "Coudons, Bedos, Montségur, Villerouge, Paradis", men på ruten kommer Villerouge (km 40) og Bedos (km 48) langt før Coudons (km 99) og Montségur (km 147) — samme mønster set på etape 6. Brugere forventer venstre-til-højre = kilometer-rækkefølge ("vennen ved siden af"-testen). Fundet af kontrolagenten, lav kompleksitet at rette. |
| OPT-006 | LAV | NY | — | **Forslag (afventer godkendelse):** "3/21 etaper afsluttet"-tælleren og "I DAG Etape 4"-badge vises samtidig på forsiden/løbssiden — uklart om dagens etape er talt med i tælleren eller ej. Fundet af kontrolagenten. Foreslår en præcisering, fx "efter dagens etape: 4/21". |
| OPT-007 | LAV | NY | — | **Forslag (afventer godkendelse):** forsidens "Seneste nyheder"-moduler viser konsekvent kun en mørk gradient + svagt kategori-ikon, aldrig et rigtigt billede, selv når artiklen har `image_url` sat. Fundet af kontrolagenten på 4/4 stikprøvede artikler. Givet brandløftet om en "lækker" oplevelse (`CLAUDE.md` §5) er dette en synlig svaghed på forsidens mest fremtrædende indhold — værd at overveje rigtige, lovligt anvendte cover-billeder (jf. §7) frem for generisk gradient. |
| OPT-008 | LAV | NY | — | **Forslag (afventer godkendelse):** når OPS-011 (402-billedfejl) er løst, bør rytter-initial-fallback'en (fx "PIDC", "SEIX" ved manglende foto) gennemgås visuelt — kontrolagenten bemærkede at den pt. vises langt oftere end tilsigtet pga. 402-buggen, hvilket har gjort dens kvalitet mere synlig end normalt. |
| SEO-017 | LAV | NY | — | **Forslag (afventer godkendelse):** tre vækst-/optimeringsforslag fra seo-agentens opfølgende GSC-kontrol (2026-07-07): (1) Skriv nye SERP-titel/beskrivelse for `/` (101 visn., 1,0% CTR, pos 8,3) og `/tour-de-france-2026` (325 visn., 0,6% CTR, pos 8,0) — begge har god position men næsten ingen klik, rent SERP-tekst-problem. (2) Prioritér den kalenderdrevne interne linkplan (jf. SEO-001) mod de 11 "Discovered – currently not indexed"-etapesider først (etape 3,5,6,7,9,10,12,15,19,20,21) — Google crawler dem allerede, så de er den mest sandsynlige gruppe til at tippe over i reel indeksering med et autoritets-skub. (3) Byg en PageSpeed Insights-integration i `agents/` til Core Web Vitals-overvågning (nævnt i seo-agentens mandat, men findes ikke endnu — ikke presserende). |
| STG-019 | HØJ | LØST | direktør | **cyclingstage.com skiftede filnavnskonvention for tour-de-france-2026's GPX-filer** fra `stage-N-route.gpx`/`stage-N.gpx` til `stage-N-parcours.gpx` — `climb_profile_generator.py`s `get_gpx_url_for_stage()`-regex matchede ikke det nye mønster og returnerede `None` for **alle** TdF 2026-etaper, hvilket stille sprang GPX-fallback-generatoren (og det nye `veloviewer_agent.py`, under opbygning) helt over for hele løbet uden nogen fejlmelding. Giro (`-route.gpx`) var upåvirket. Fundet under opbygning af `veloviewer_agent.py`s test. Rettet: regex udvidet til `(?:-route|-parcours)?`. Verificeret: GPX-udtræk for Côte de Santa Creu d'Olorda (etape 2) giver nu 414 punkter. **Ikke kørt for hele TdF endnu** — bør genkøres (`climb_profile_generator.py --race tour-de-france-2026 --all`) for at fange evt. stigninger, der i mellemtiden er faldet tilbage på et forkert/manglende billede pga. denne bug. |
| STG-020 | HØJ | TILDELT | stigningsagent | **Ny VeloViewer-baseret stigningsprofil (prioritet 1) bygget og testkørt** — `agents/veloviewer_agent.py` + `agents/veloviewer_strava_api.py`, se `docs/superpowers/specs/2026-07-07-veloviewer-climb-profiles-design.md`. Finder Strava-segment via det officielle `/segments/explore`-API (ingen login/browser), verificerer mod DB (tolerance + navnetjek). Undervejs afsløret og rettet en reel fejlklasse: numerisk tolerance alene godkendte "Col d'Aspin" mod segmentet "Ste Marie - Tourmalet 10kms" (nabo-klatre, samme fejlklasse som STG-004/STG-009) — rettet med `name_plausible_match()`. Testkørt (dry-run) mod tour-de-france-2026 etape 6: Côte de Mauvezin og Col du Tourmalet verificeret matchet; Côte de Loucroup, Col d'Aspin, Gavarnie-Gèdre korrekt uden match (falder tilbage til eksisterende pipeline). **Mangler før produktion:** (1) `cykel-frontend/app/[slug]/stage/[n]/ClimbProfile.tsx`-ændringen (render VeloViewer-iframe når `veloviewer_segment_id` er sat) er ikke bygget endnu, (2) `api.py:569` mangler `veloviewer_segment_id` i `select`-listen, (3) `--write-db` er ikke kørt mod produktion endnu — afventer ejerens godkendelse. Ny DB-kolonne `stage_climbs.veloviewer_segment_id` (nullable integer) allerede tilføjet. |
| SEO-012 | MIDDEL | LØST | direktør | **Rettede Search Consoles "strukturerede data"-advarsler (Event/SportsEvent-schema) på løbs- og etapesider.** Ejeren modtog to GSC-notifikationer: 2 begivenheder manglede feltet `name` i `location` og `url` i `organizer` (nyeste), samt 8 begivenheder manglede `description`/`image`/`performer`/`offers`/`organizer` (27. juni, bredere sæt — ikke-kritiske forslag, blokerer ikke visning i Google). Rodårsag fundet i koden: `cykel-frontend/app/[slug]/page.tsx`s JSON-LD (`SportsEvent`) manglede `location.name` helt og `organizer.url`; `cykel-frontend/app/[slug]/stage/[n]/page.tsx`s JSON-LD kunne i sjældne tilfælde sætte `location.name` til en tom streng (når både start- og finish-lokation manglede) og havde slet ikke `organizer`. Rettet: begge tilføjet `location.name` (etape-siden falder nu tilbage til løbsnavnet i stedet for tom streng), `organizer: { name: "UCI WorldTour", url: "https://www.uci.org" }`, samt `description` (fra `race.description`/en genereret etape-beskrivelse i samme stil som eksisterende SEO-metadata) og `image` (fra `cover_image_url`/`elevation_image_url`, når til stede). `performer`/`offers` bevidst ikke tilføjet — cykelløb har ingen billetter/enkeltoptrædende at angive, og at opfinde data ville bryde CLAUDE.md §6's korrekthedskrav. Verificeret med `npx tsc --noEmit` (grønt). **Ikke deployet/verificeret live endnu** — afventer næste deploy + at Google re-crawler og lukker notifikationerne selv (kan tage dage-uger). |
